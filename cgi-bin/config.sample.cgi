#!/usr/local/bin/perl
# Rename to config.cgi and edit following values to your needs. leading comments are expected, no $ signs. This is for security so noone can see your config
#giga_tmp_dir=/tmp/gigaupload
#giga_max_upload=50000000
1;
