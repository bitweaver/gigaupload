#!/usr/local/bin/perl
# Rename to config.cgi and edit following values to your needs. leading comments are expected, no $ signs
#tmp_dir=/tmp/gigaupload
#post_url=/gigaupload/
#interval=1
#max_upload=50000000

1;
